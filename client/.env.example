# ------------------------------------------------------
# MS&A Demo — Client (Vite)
# ------------------------------------------------------
# REQUIRED: Your Cesium ion access token. Do NOT commit the real token.
# Copy this file to client/.env and paste your token value.
VITE_CESIUM_ION_TOKEN=__PUT_YOUR_CESIUM_ION_TOKEN_HERE__

# Base URL the frontend uses for API calls.
# In dev we use Vite's proxy so "/api" is correct; in production the server
# serves the SPA and the API from the same origin, so "/api" is still correct.
VITE_API_BASE_URL=/api

# Example: optional build metadata (safe to remove if unused)
# VITE_APP_NAME=MSA Demo
# VITE_LOG_LEVEL=info
