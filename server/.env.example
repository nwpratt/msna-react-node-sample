# MS&A Demo — Server (Express)
NODE_ENV=development
PORT=5001

# Allow the Vite dev server during local development
CORS_ORIGIN=http://localhost:5173

# Where simulation JSON files are stored (ensure this folder exists)
SIMS_DIR=./data/sims

# Body size limit for JSON uploads
JSON_LIMIT=2mb
